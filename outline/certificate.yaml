apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: outline
spec:
  secretName: outline-tls
  dnsNames:
    - outline.byulmaru.co
  issuerRef:
    name: cert-issuer
    kind: ClusterIssuer
